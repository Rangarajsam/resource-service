{
    "name": "MovieCrud",
    "version": "0.0.1",
    "description": "Crud App Using Resource",
    "devDependencies": {
        "http-server": "^0.6.1"
    },
    "scripts": {
        "prestart": "npm install",
        "start": "http-server -a localhost -p 8000 -c-1"

    }
}